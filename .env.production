# Production Environment Configuration
# Copy this file to production server as .env and update values

# Database (PostgreSQL)
DATABASE_URL=postgresql://destek_user:destek_pass@db:5432/destek_db

# Frontend CORS - HTTPS domain
CORS_ORIGINS=https://destek.tesmer.org.tr,https://localhost

# File uploads
UPLOAD_DIR=/app/uploads

# LDAP Settings (if enabled)
LDAP_SERVER=
LDAP_PORT=389
LDAP_USERNAME=
LDAP_PASSWORD=
LDAP_BASE_DN=

# Email Settings (configured via System Settings panel)
# Or can be overridden here:
SMTP_SERVER=mail.tesmer.org.tr
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=support@tesmer.org.tr

# Frontend API URL (usually auto-detected, but can be set explicitly)
# REACT_APP_API_URL=https://destek.tesmer.org.tr/api

# Security
SECRET_KEY=your-super-secret-key-change-this-in-production
