server {
    listen 3000;
    server_name _;

    # Maksimum dosya yükleme boyutu (10MB)
    client_max_body_size 100M;

    root /usr/share/nginx/html;
    index index.html;

    # API requests - proxy to backend container
    location /api/ {
        proxy_pass http://destek-backend-1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Uploads - proxy to backend container
    # ^~ prefix: regex location'lardan önce match et (statik cache kuralına düşmesin)
    location ^~ /uploads/ {
        proxy_pass http://destek-backend-1:8000/uploads/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Frontend static files
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
